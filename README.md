# ND06&VL53L8CX

## 本地地址
E:\HTC\XM2528\XM2528TEST\VL53

## 开发进程
### 年前
VL53L8CX单独能运行
ND06能单独运行
### 2026/2/28
- 单独运行ND06， 
- 与电梯MCU通信， 将开门信号通过485通信传输，115200pbs，偶检验，9bit，1停止位

### 2026/3/10
- 启动system stick的中断，才能使用原有5ms标记
- ND06迁移忘记给模块上电，导致IIC握手无应答（第九时序）
- 代码中使用了 printf 之类的标准库函数，但没有正确地将其输出重定向到硬件外设（如串口），需要多次点击“运行”程序才能启动，这通常不是一个硬件故障，而是一个经典的、由半主机模式（Semihosting） 引起的软件问题
- 两个传感器能同时进行

### 2026/3/11
- 使用Xtalk进行校准，ST的demo可以正常校准，但是我们的开发板在平铺滤波片时无法完成校准

## 2026/3/12
- 两个485要关闭overrun，从MX修改，否则无法正常建立通信
- 添加了PDA和MPU通信之后，两个模组无法同时运行


## 2026/3/13
- 不校准无法使用平铺滤光片
- 斜铺滤光片的4*4可以检测轿厢内人员，但是最里面边缘不能检测到
- 斜铺滤光片的8*8检测轿厢内人员，最里面的数据不稳定
- 通过将通信地址改为58可以显示VL53L8CX的测量结果
- 将数据通过改变关门时间阈值进行分别显示8*8分辨率的测试结果，分为四个象限


## FLASH消耗情况
### 20260212加入了VL53L8CX的API,优化O0
```
Program Size: Code=40652 RO-data=88548 RW-data=12 ZI-data=6436  
```
根据 Keil 编译输出的信息，程序占用的 **Flash 大小 = Code + RO-data + RW-data**（因为 RW-data 的初始值存储在 Flash 中，运行时才复制到 RAM）。

```
Code        = 40652 字节
RO-data     = 88548 字节
RW-data     = 12 字节
-------------------------
Flash 占用  = 40652 + 88548 + 12 = 129212 字节
```

**换算**：  
129212 字节 = 129212 ÷ 1024 ≈ **126.2 KB**

**各部分含义**：  
- **Code**：程序代码（.text 段），占 Flash  
- **RO-data**：只读数据（常量、const 变量等），占 Flash  
- **RW-data**：已初始化全局变量，其初始值存储在 Flash 中，运行时被复制到 RAM，因此**初始值部分也占 Flash**  
- **ZI-data**：未初始化全局变量，仅占 RAM，**不占用 Flash**

所以最终的 Flash 占用是 **129212 字节**（约 126.2 KB）。

优化策略| O0 | O3 | OZ
--------
Code   |40652 | |



### 20260311
- O3优化策略：Program Size: Code=37266 RO-data=88502 RW-data=32 ZI-data=6976  
- 
